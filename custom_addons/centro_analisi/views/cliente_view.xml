<odoo>

    <record id="view_cliente_list" model="ir.ui.view">
        <field name="name">res.partner.list</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <list string="Clienti">
                <field name="name" string="Nome Completo"/>
                <field name="email" string="Email"/>
                <field name="phone" string="Telefono"/>
                <field name="analisi_ids" string="Storico Analisi"/>
                <control>
                    <create string="Add a item"/>
                    <create string="Add a section" context="{'default_type': 'section'}"/>
                    <create string="Add a note" context="{'default_type': 'note'}"/>
                </control>
            </list>
        </field>
    </record>

    <record id="view_cliente_form" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <form string="res.partner">
                <sheet>
                    <group>
                        <field name="name" string="Nome Completo"/>
                        <field name="email" string="Email"/>
                        <field name="phone" string="Telefono"/>
                        <field name="analisi_ids" string="Storico Analisi"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
</odoo>